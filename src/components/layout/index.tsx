import styles from './layout.module.scss'

function Layout() {
  return (
    <div className={styles['wrapper']}>
      <p className={styles['wrapper--title']}>hossein</p>
    </div>
  )
}

export default Layout
